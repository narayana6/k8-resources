apiVersion: v1
kind: Pod
metadata:
  name: pod-config
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: bnsaws
    # env:
    # - name: course
    #   valueFrom:
    #     configMapKeyRef:
    #       name: bnsaws
    #       key: course
    # - name: trainer
    #   valueFrom:
    #     configMapKeyRef:
    #       name: bnsaws
    #       key: trainer
